import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint config moved to eslint.config.mjs in Next.js 16
  // The `eslint` key is no longer supported in next.config.ts
};

export default nextConfig;
